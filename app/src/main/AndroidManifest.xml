<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="androidseminar.speechtotext">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_mic"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_mic_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- TODO: 4. Change Activity to MainActivityNoDialog -->
        <activity android:name=".MainActivityNoDialog">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>